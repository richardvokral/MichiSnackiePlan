# Neon Postgres — set via Vercel Marketplace integration
DATABASE_URL=postgres://user:pass@host/dbname

# Bootstrap admin — always has admin access even without being in the admins table
FIRST_ADMIN_EMAIL=richard.vokral@gmail.com

# Auth gate — set to "true" once Logto is wired up
AUTH_ENABLED=false

# Vercel Blob — set via Vercel Blob integration
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# Logto PLACEHOLDERS — not implemented yet  TODO(LOGTO)
LOGTO_ENDPOINT=
LOGTO_APP_ID=
LOGTO_APP_SECRET=
LOGTO_COOKIE_SECRET=
LOGTO_BASE_URL=
